<Window x:Class="StudentsManagerApp_.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:StudentsManagerApp_.ViewModels"
        xmlns:m="clr-namespace:StudentsManagerApp_.Models"
        
        Title="MainWindow" 
        
        Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <DataTemplate x:Key="Template_Student"
                      DataType="{x:Type TypeName=m:Student}">
            <StackPanel Orientation="Vertical">
                <TextBlock>
                    <TextBlock.Text>
                        <MultiBinding StringFormat="#{0}: {1}">
                            <Binding Path="Id"/>
                            <Binding Path="FullName"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <ListBox ItemsSource="{Binding Path=Marks}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="#{0}: [{1}] {2} {3}">
                                        <Binding Path="Id"/>
                                        <Binding Path="Value"/>
                                        <Binding Path="Teacher"/>
                                        <Binding Path="Subject"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Загрузить"
                      Command="{Binding Path=LoadCommand}"/>
        </Menu>
        
        <ListBox ItemsSource="{Binding Path=Students}"
                 ItemTemplate="{StaticResource ResourceKey=Template_Student}"/>
    </DockPanel>
</Window>
